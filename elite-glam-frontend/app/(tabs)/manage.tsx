import React from 'react';
import ManageProductsScreen from '../(store)/manage-products';

/**
 * This component acts as a wrapper to display the ManageProductsScreen
 * within the main tab navigation layout for shop owners.
 */
export default function ManageTabScreen() {
  return <ManageProductsScreen />;
}
